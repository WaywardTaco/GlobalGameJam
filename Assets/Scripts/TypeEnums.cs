
namespace Enums {
    public enum WorldEventType {
        None, 
    }
    
    public enum UpgradeType {
        None, 
    }
    
    public enum ResourceType {
        None,
    }

    public enum StockType {
        None,
    }

}